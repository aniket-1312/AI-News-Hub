name: ai_news_hub
description: AI News Hub Flutter App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0

flutter:
  uses-material-design: true
